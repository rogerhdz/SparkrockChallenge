{
  "name": "C# .NET 8 Architect Environment",
  "image": "mcr.microsoft.com/dotnet/sdk:8.0",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csdevkit",
        "ms-dotnettools.csharp",
        "kavya-manohar.openspec"
      ]
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y nodejs npm && npm config set strict-ssl false && npm install -g @fission-ai/openspec && dotnet tool install -g dotnet-ef"
}